# ✅ ЧЕКЛИСТ ДЕЙСТВИЙ ДЛЯ ИСПРАВЛЕНИЯ RECON

## 🎯 Цель: Увеличить Success Rate с 0% до 87%

---

## ✅ ШАГ 1: ПРОВЕРКА ПРИМЕНЁННЫХ ИСПРАВЛЕНИЙ

### Проверьте, что исправления применены:

```bash
# Проверка исправления telemetry
grep -A 5 "Update aggregate telemetry" core/bypass/engine/base_engine.py
```

Должно быть:
```python
# Update aggregate telemetry
self._telemetry['aggregate']['segments_sent'] += len(specs)
self._telemetry['aggregate']['fake_packets_sent'] += fake_count
```

```bash
# Проверка исправления checksum
grep -A 3 "WINDIVERT_FLAG_NO_CHECKSUM" core/bypass/packet/sender.py
```

Должно быть:
```python
w.send(pkt, flags=0x0001)  # WINDIVERT_FLAG_NO_CHECKSUM
```

**Результат:** 
- [ ] Telemetry исправление найдено
- [ ] Checksum исправление найдено

---

## ✅ ШАГ 2: ЗАПУСК АВТОМАТИЧЕСКОГО ТЕСТА

```bash
cd recon
python test_critical_fixes.py
```

**Ожидаемый вывод:**
```
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!

Исправления работают:
  ✅ Telemetry обновляется
  ✅ PCAP корректный
```

**Результат:**
- [ ] Тест пройден успешно
- [ ] Telemetry > 0
- [ ] PCAP корректный

**Если тест провален, переходите к Шагу 6 (Диагностика)**

---

## ✅ ШАГ 3: РУЧНАЯ ПРОВЕРКА TELEMETRY

```bash
cd recon
python cli.py x.com --debug --strategy "--dpi-desync=fake,fakeddisorder --dpi-desync-split-pos=3 --dpi-desync-fooling=badsum,badseq --dpi-desync-ttl=3"
```

Дождитесь завершения (~60 секунд)

```bash
# Проверка telemetry
python -c "import json; d=json.load(open('recon_summary.json')); t=d['all_results'][0]['engine_telemetry']; print('segments_sent:', t['segments_sent']); print('fake_packets_sent:', t['fake_packets_sent']); print('Success:', 'OK' if t['segments_sent'] > 0 else 'FAIL')"
```

**Ожидаемый вывод:**
```
segments_sent: 3
fake_packets_sent: 1
Success: OK
```

**Результат:**
- [ ] segments_sent > 0
- [ ] fake_packets_sent > 0

---

## ✅ ШАГ 4: АНАЛИЗ PCAP

```bash
python deep_global_diagnosis.py
```

**Проверьте вывод:**

1. **Fake packet:**
   - [ ] TTL = 3 ✅
   - [ ] Checksum = 0xDEAD ✅ (или другой испорченный)
   - [ ] Length = 517-680 байт

2. **Real packets:**
   - [ ] TTL = 64 ✅ (НЕ 128!)
   - [ ] Checksum = правильный ✅

3. **Количество Client Hello:**
   - [ ] 1-2 пакета (идеально: 1)

4. **RST пакеты:**
   - [ ] 0-1 RST (идеально: 0)

---

## ✅ ШАГ 5: ПРОВЕРКА SUCCESS RATE

```bash
python -c "import json; d=json.load(open('recon_summary.json')); print('Success Rate:', d['success_rate'], '%'); print('Working Strategies:', d['working_strategies_found'])"
```

**Ожидаемый результат:**
- [ ] Success Rate > 0% (было 0%)
- [ ] Working Strategies > 0 (было 0)

**Идеальный результат:**
- [ ] Success Rate ≈ 87% (как у Zapret)

---

## ✅ ШАГ 6: ДИАГНОСТИКА ПРОБЛЕМ (если что-то не работает)

### Проблема: Telemetry все еще 0

```bash
# Проверка кода
grep -n "segments_sent" core/bypass/engine/base_engine.py | head -20

# Проверка логов
grep "📤" log.txt | head -10
grep "ERROR" log.txt
```

**Действия:**
- [ ] Убедитесь, что используется правильная версия кода
- [ ] Проверьте, что пакеты отправляются (должны быть "📤" в логах)
- [ ] Проверьте ошибки в логах

### Проблема: Checksum не испорчен

```bash
# Проверка версии pydivert
python -c "import pydivert; print('pydivert version:', pydivert.__version__)"

# Проверка поддержки flags
python -c "import inspect; import pydivert; sig=inspect.signature(pydivert.WinDivert.send); print('send signature:', sig)"
```

**Действия:**
- [ ] Проверьте версию pydivert (должна быть >= 2.1.0)
- [ ] Если `flags` не поддерживается, checksum будет пересчитан (известное ограничение)
- [ ] Рассмотрите обновление pydivert или использование raw sockets

### Проблема: Success Rate все еще 0%

```bash
# Проверка RST пакетов
grep "RST" log.txt

# Проверка timeout
grep "TIMEOUT" log.txt

# Проверка SSL errors
grep "SSL" log.txt
```

**Действия:**
- [ ] Если много RST - проблема в логике fakeddisorder
- [ ] Если много TIMEOUT - проблема в сети или DPI
- [ ] Если SSL errors - проблема в TLS handshake

---

## ✅ ШАГ 7: СРАВНЕНИЕ С ZAPRET

```bash
# Запустите Zapret с той же стратегией
# Сохраните PCAP как zapret_new.pcap

# Сравните
python deep_global_diagnosis.py
```

**Сравните:**
- [ ] Количество пакетов
- [ ] TTL значения
- [ ] Checksum значения
- [ ] Длину fake packets
- [ ] Последовательность пакетов

---

## ✅ ШАГ 8: ДОПОЛНИТЕЛЬНЫЕ ИСПРАВЛЕНИЯ (если нужно)

### Если Client Hello дублируется:

**Файл:** `core/bypass/techniques/primitives.py`
**Метод:** `apply_fakeddisorder()`

Проверьте логику создания overlap пакета.

### Если длина fake packet различается:

Выполните hex-dump анализ:
```bash
python -c "from scapy.all import *; pkts=rdpcap('recon_x1.pcap'); [hexdump(p) for p in pkts if TCP in p and Raw in p][:1]"
```

Сравните с Zapret.

---

## 📊 МЕТРИКИ УСПЕХА

### Минимальные требования (Шаг 1):
- ✅ segments_sent > 0
- ✅ fake_packets_sent > 0
- ✅ Success Rate > 0%

### Хорошие результаты (Шаг 2):
- ✅ Success Rate > 50%
- ✅ RST Count < 2
- ✅ Client Hello = 1

### Отличные результаты (Цель):
- ✅ Success Rate ≈ 87%
- ✅ RST Count = 0
- ✅ Полное соответствие с Zapret

---

## 📝 ОТЧЁТНОСТЬ

После каждого шага сохраняйте:
- [ ] recon_summary.json
- [ ] log.txt
- [ ] recon_*.pcap
- [ ] DEEP_DIAGNOSIS_REPORT.json

Это поможет в дальнейшей диагностике.

---

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ

1. Откройте issue с приложением:
   - recon_summary.json
   - log.txt (первые и последние 100 строк)
   - DEEP_DIAGNOSIS_REPORT.json
   - Версия Python, pydivert, Windows

2. Опишите:
   - Какие шаги выполнены
   - Какие результаты получены
   - Что ожидалось

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [ ] Шаг 1: Исправления применены
- [ ] Шаг 2: Автоматический тест пройден
- [ ] Шаг 3: Telemetry > 0
- [ ] Шаг 4: PCAP корректный
- [ ] Шаг 5: Success Rate > 0%
- [ ] Шаг 6: Диагностика (если нужно)
- [ ] Шаг 7: Сравнение с Zapret
- [ ] Шаг 8: Дополнительные исправления (если нужно)

---

## 🎯 ЦЕЛЬ ДОСТИГНУТА?

- [ ] Success Rate ≈ 87%
- [ ] Recon работает как Zapret
- [ ] Все тесты пройдены

**Поздравляем! 🎉**

---

Конец чеклиста.
