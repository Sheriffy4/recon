# Фаза 8: Система адаптивного обучения и кэширования

## Обзор

Фаза 8 добавляет мощную систему адаптивного обучения, которая накапливает знания о производительности стратегий и использует их для оптимизации будущих тестов.

## Ключевые компоненты

### 1. AdaptiveLearningCache
Основной класс системы обучения:
- **Кэширование результатов**: Сохраняет производительность каждой стратегии для каждого домена/IP
- **Паттерны доменов**: Изучает, какие типы стратегий работают лучше для конкретных доменов
- **Паттерны DPI**: Анализирует эффективность стратегий против различных типов DPI
- **Экспоненциальное сглаживание**: Обновляет оценки с учетом новых данных

### 2. StrategyPerformanceRecord
Структура данных для хранения информации о производительности:
```python
@dataclass
class StrategyPerformanceRecord:
    strategy: str              # Полная строка стратегии
    domain: str               # Домен
    ip: str                   # IP-адрес
    success_rate: float       # Процент успеха (0.0-1.0)
    avg_latency: float        # Средняя задержка в мс
    timestamp: str            # Время последнего обновления
    dpi_fingerprint_hash: str # Хэш фингерпринта DPI
    test_count: int           # Количество тестов
```

### 3. Умная сортировка стратегий
Система оптимизирует порядок тестирования стратегий на основе:
- **60%** - Прямые предсказания по истории (точное совпадение стратегии+домена+IP)
- **25%** - Рекомендации по домену (какие типы стратегий работают для этого домена)
- **15%** - Рекомендации по DPI (какие стратегии эффективны против этого типа DPI)

## Новые возможности

### Командная строка
```bash
# Показать статистику кэша
python cli.py --cache-stats

# Очистить кэш обучения
python cli.py --clear-cache

# Отключить обучение для текущего запуска
python cli.py rutracker.org --disable-learning
```

### Автоматическая оптимизация
- При каждом запуске система автоматически применяет накопленные знания
- Рабочие стратегии тестируются в первую очередь
- Неэффективные стратегии откладываются на потом

### Персистентность данных
- Кэш сохраняется в файл `recon_learning_cache.pkl`
- Данные сохраняются между запусками программы
- Автоматическая загрузка при старте

## Интеграция с существующими режимами

### Гибридный режим
- Оптимизирует порядок тестирования стратегий
- Сохраняет результаты всех тестов
- Показывает рекомендации в отчете

### Эволюционный режим
- Использует накопленные знания для создания лучшей начальной популяции
- Приоритизирует проверенные типы стратегий
- Сохраняет результаты эволюции для будущих запусков

## Статистика и аналитика

### Отчеты включают:
- Общее количество записей в кэше
- Количество выполненных тестов
- Изученные домены и DPI-паттерны
- Средний процент успеха
- Топ-стратегии для текущего домена

### Пример вывода:
```
🧠 Adaptive Learning Stats
Strategy records: 15
Total tests performed: 23
Domains learned: 3
DPI patterns: 2
Average success rate: 65.2%
Top strategies for rutracker.org: multisplit(100.0%), seqovl(85.0%), badsum(0.0%)
```

## Алгоритмы обучения

### Экспоненциальное сглаживание
```python
# Коэффициент обучения α = 0.3
new_rate = α * current_result + (1 - α) * historical_rate
```

### Учет возраста данных
```python
# Снижение доверия к старым данным (через неделю)
age_hours = (now - record_time).total_seconds() / 3600
confidence = max(0.1, 1.0 - age_hours / (24 * 7))
```

## Производительность

### Преимущества:
- **Быстрее находит рабочие стратегии**: Тестирует проверенные варианты в первую очередь
- **Меньше неудачных попыток**: Избегает заведомо неэффективных стратегий
- **Улучшается со временем**: Чем больше тестов, тем точнее предсказания
- **Адаптируется к изменениям**: Обновляет оценки при изменении условий

### Оптимизации:
- Кэш загружается только один раз при старте
- Сохранение происходит только при изменениях
- Хэширование стратегий для быстрого поиска
- Ограничение размера рекомендаций

## Примеры использования

### Первый запуск (пустой кэш):
```bash
python cli.py rutracker.org --count 10
# Тестирует все 10 стратегий, сохраняет результаты
```

### Повторный запуск (с кэшем):
```bash
python cli.py rutracker.org --count 10
# Загружает кэш, оптимизирует порядок
# Рабочие стратегии тестируются первыми
```

### Анализ накопленных данных:
```bash
python cli.py --cache-stats
# Показывает статистику обучения
```

## Будущие улучшения

1. **Машинное обучение**: Интеграция ML-моделей для предсказания эффективности
2. **Кластеризация доменов**: Группировка похожих доменов для лучших рекомендаций
3. **Временные паттерны**: Учет времени суток и дня недели
4. **Сетевые условия**: Адаптация к изменениям в сетевой инфраструктуре
5. **Федеративное обучение**: Обмен знаниями между пользователями (анонимно)

## Заключение

Система адаптивного обучения значительно повышает эффективность поиска рабочих стратегий обхода DPI. Она превращает каждый запуск программы в возможность для обучения, постепенно создавая персонализированную базу знаний для каждого пользователя.

Фаза 8 завершает создание полноценной интеллектуальной системы обхода DPI, которая не только находит рабочие стратегии, но и учится на опыте для более эффективной работы в будущем.