# 🎯 ПРАВИЛЬНЫЙ СИНТАКСИС СТРАТЕГИЙ

## Дата: 2025-10-03

---

## ❌ НЕПРАВИЛЬНО (НЕ РАБОТАЕТ):

```bash
# ЭТО НЕ РАБОТАЕТ!
python cli.py x.com --strategy "split(pos=3)"
python cli.py x.com --strategy "disorder(split_pos=3)"
```

**Ошибка:**
```
[ERROR] No valid DPI methods found in strategy: 'split(pos=3)'
Fatal Error: No valid strategies could be parsed.
```

---

## ✅ ПРАВИЛЬНО (РАБОТАЕТ):

### Формат zapret:
```bash
--dpi-desync=METHOD [--dpi-desync-PARAM=VALUE ...]
```

### Примеры рабочих стратегий:

#### 1. Simple Split (простое разделение)
```bash
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=3" --pcap test_split.pcap
```

#### 2. Disorder (перестановка)
```bash
python cli.py x.com --strategy "--dpi-desync=disorder --dpi-desync-split-pos=3" --pcap test_disorder.pcap
```

#### 3. Fake (фальшивый пакет)
```bash
python cli.py x.com --strategy "--dpi-desync=fake --dpi-desync-ttl=1" --pcap test_fake.pcap
```

#### 4. Fakeddisorder (fake + disorder)
```bash
python cli.py x.com --strategy "--dpi-desync=fake,disorder --dpi-desync-split-pos=76 --dpi-desync-ttl=3" --pcap test_fakeddisorder.pcap
```

#### 5. Multisplit (множественное разделение)
```bash
python cli.py x.com --strategy "--dpi-desync=multisplit --dpi-desync-split-count=3" --pcap test_multisplit.pcap
```

#### 6. Multidisorder (множественная перестановка)
```bash
python cli.py x.com --strategy "--dpi-desync=multidisorder --dpi-desync-split-count=3" --pcap test_multidisorder.pcap
```

---

## 📋 ДОСТУПНЫЕ МЕТОДЫ (DPI-DESYNC):

| Метод | Описание | Параметры |
|-------|----------|-----------|
| `split` | Простое разделение | `--dpi-desync-split-pos=N` |
| `disorder` | Перестановка пакетов | `--dpi-desync-split-pos=N` |
| `fake` | Фальшивый пакет | `--dpi-desync-ttl=N` |
| `fake,disorder` | Fake + disorder | `--dpi-desync-split-pos=N --dpi-desync-ttl=N` |
| `multisplit` | Множественное разделение | `--dpi-desync-split-count=N` |
| `multidisorder` | Множественная перестановка | `--dpi-desync-split-count=N` |

---

## 📋 ДОСТУПНЫЕ ПАРАМЕТРЫ:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--dpi-desync-split-pos=N` | Позиция разделения | `--dpi-desync-split-pos=3` |
| `--dpi-desync-ttl=N` | TTL для fake пакетов | `--dpi-desync-ttl=1` |
| `--dpi-desync-split-count=N` | Количество разделений | `--dpi-desync-split-count=3` |
| `--dpi-desync-fooling=METHOD` | Метод обмана | `--dpi-desync-fooling=badsum` |
| `--dpi-desync-split-seqovl=N` | Overlap size | `--dpi-desync-split-seqovl=336` |

---

## 🚀 РЕКОМЕНДУЕМЫЕ ТЕСТЫ:

### Тест 1: Split с разными позициями
```bash
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=1" --pcap test_split_1.pcap
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=3" --pcap test_split_3.pcap
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=5" --pcap test_split_5.pcap
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=10" --pcap test_split_10.pcap
```

### Тест 2: Disorder с разными позициями
```bash
python cli.py x.com --strategy "--dpi-desync=disorder --dpi-desync-split-pos=1" --pcap test_disorder_1.pcap
python cli.py x.com --strategy "--dpi-desync=disorder --dpi-desync-split-pos=3" --pcap test_disorder_3.pcap
python cli.py x.com --strategy "--dpi-desync=disorder --dpi-desync-split-pos=5" --pcap test_disorder_5.pcap
```

### Тест 3: Fake с разными TTL
```bash
python cli.py x.com --strategy "--dpi-desync=fake --dpi-desync-ttl=1" --pcap test_fake_ttl1.pcap
python cli.py x.com --strategy "--dpi-desync=fake --dpi-desync-ttl=2" --pcap test_fake_ttl2.pcap
python cli.py x.com --strategy "--dpi-desync=fake --dpi-desync-ttl=3" --pcap test_fake_ttl3.pcap
```

### Тест 4: Fakeddisorder с TTL=1
```bash
python cli.py x.com --strategy "--dpi-desync=fake,disorder --dpi-desync-split-pos=76 --dpi-desync-ttl=1 --dpi-desync-fooling=badsum" --pcap test_fakeddisorder_ttl1.pcap
```

### Тест 5: Multisplit
```bash
python cli.py x.com --strategy "--dpi-desync=multisplit --dpi-desync-split-count=2" --pcap test_multisplit_2.pcap
python cli.py x.com --strategy "--dpi-desync=multisplit --dpi-desync-split-count=3" --pcap test_multisplit_3.pcap
python cli.py x.com --strategy "--dpi-desync=multisplit --dpi-desync-split-count=4" --pcap test_multisplit_4.pcap
```

---

## 📊 АНАЛИЗ РЕЗУЛЬТАТОВ:

После каждого теста запускайте:
```bash
python analyze_bypass_failure.py
```

Смотрите на:
- Количество RST пакетов (меньше = лучше)
- Количество Server Hello (больше = лучше)
- TTL распределение
- Успешность соединения

---

## 💡 СТРАТЕГИЯ ПОИСКА:

1. **Начните с простых методов:**
   - split
   - disorder
   - fake

2. **Попробуйте разные параметры:**
   - split_pos: 1, 3, 5, 10, 76
   - ttl: 1, 2, 3
   - split_count: 2, 3, 4

3. **Комбинируйте методы:**
   - fake,disorder
   - fake,split
   - multisplit
   - multidisorder

4. **Анализируйте результаты:**
   - Какой метод дает меньше RST?
   - Какой метод дает больше Server Hello?
   - Какая комбинация работает лучше?

---

## ✅ ПРИМЕР ПОЛНОГО ТЕСТА:

```bash
# 1. Split pos=3
python cli.py x.com --strategy "--dpi-desync=split --dpi-desync-split-pos=3" --pcap test_split_3.pcap
python analyze_bypass_failure.py

# 2. Disorder pos=3
python cli.py x.com --strategy "--dpi-desync=disorder --dpi-desync-split-pos=3" --pcap test_disorder_3.pcap
python analyze_bypass_failure.py

# 3. Fake TTL=1
python cli.py x.com --strategy "--dpi-desync=fake --dpi-desync-ttl=1" --pcap test_fake_ttl1.pcap
python analyze_bypass_failure.py

# 4. Fakeddisorder TTL=1
python cli.py x.com --strategy "--dpi-desync=fake,disorder --dpi-desync-split-pos=76 --dpi-desync-ttl=1 --dpi-desync-fooling=badsum" --pcap test_fakeddisorder_ttl1.pcap
python analyze_bypass_failure.py

# 5. Сравнить результаты
# Выбрать стратегию с минимальным RST и максимальным Server Hello
```

---

**ВАЖНО:** Используйте формат zapret (`--dpi-desync=...`), а не короткий формат!
