╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║              🎯 ПЛАН ТЕСТИРОВАНИЯ ВСЕХ АТАК В ПРОЕКТЕ 🎯                     ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

Дата: 2025-10-04
Статус: 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА ОБНАРУЖЕНА
Приоритет: 🔥🔥🔥 БЛОКИРУЮЩАЯ

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА: Парсер стратегий не распознает синтаксис

ОШИБКИ:
  [ERROR] No valid DPI methods found in strategy: 'fake(ttl=1, fooling=['badsum'])'
  Warning: Could not parse strategy: fake(ttl=1, fooling=['badsum'])
  Fatal Error: No valid strategies could be parsed.

ПРОВАЛИВШИЕСЯ ТЕСТЫ:
  ❌ fake(ttl=1, fooling=['badsum'])
  ❌ split(split_pos=3)
  ❌ split(split_pos=1)
  ❌ fake(ttl=64, fooling=['badsum'])
  ❌ fakeddisorder(split_pos=76, overlap_size=336, ttl=3)

РЕЗУЛЬТАТ:
  • Все тесты провалились на этапе парсинга
  • Пакеты не генерируются
  • Валидация невозможна
  • Блокирует все дальнейшее тестирование

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔍 КОРНЕВАЯ ПРИЧИНА                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

ПРИЧИНА #1: ПАРСЕР ОЖИДАЕТ ДРУГОЙ СИНТАКСИС

  Предоставлено:
    fake(ttl=1, fooling=['badsum'])
    split(split_pos=1)
  
  Ожидается:
    --dpi-desync=fake --dpi-desync-ttl=1 --dpi-desync-fooling=badsum
    --dpi-desync=split --dpi-desync-split-pos=1

  Проблема:
    • Парсер понимает только Zapret-style синтаксис
    • Function-style синтаксис не поддерживается
    • Нет конвертера между форматами

ПРИЧИНА #2: ОТСУТСТВУЕТ КОНВЕРТЕР СИНТАКСИСА

  Система имеет ДВА стиля синтаксиса:
    1. Function-style: fake(ttl=1, fooling=['badsum'])
    2. Zapret-style: --dpi-desync=fake --dpi-desync-ttl=1
  
  Но:
    • Нет автоматического конвертера
    • Парсер понимает только один формат
    • CLI генерирует другой формат

ПРИЧИНА #3: РЕЕСТР И ПАРСЕР НЕ СИНХРОНИЗИРОВАНЫ

  Реестр атак:
    • Содержит 159 атак
    • Использует function-style
  
  Парсер:
    • Имеет хардкод список атак
    • Использует zapret-style
    • Не синхронизирован с реестром

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 РЕШЕНИЕ: 5 ФАЗ                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

ФАЗА 1: ИСПРАВИТЬ ПАРСЕР СТРАТЕГИЙ (КРИТИЧНО)
  
  Задачи:
    1. Создать StrategyParserV2 с поддержкой обоих синтаксисов
    2. Реализовать function-style парсер
    3. Реализовать parameter парсер
    4. Добавить валидацию параметров
    5. Интегрировать с существующей системой
  
  Время: 6-8 часов (1 день)
  Файл: recon/core/strategy_parser_v2.py

ФАЗА 2: СОЗДАТЬ ВАЛИДАТОР ПАКЕТОВ
  
  Задачи:
    1. Создать PacketValidator класс
    2. Реализовать валидацию sequence numbers
    3. Реализовать валидацию checksums
    4. Реализовать валидацию TTL
    5. Создать генератор визуальных diff
  
  Время: 6-8 часов (1 день)
  Файл: recon/core/packet_validator.py

ФАЗА 3: СОЗДАТЬ ОРКЕСТРАТОР ТЕСТОВ
  
  Задачи:
    1. Создать AttackTestOrchestrator класс
    2. Загрузить все атаки из реестра
    3. Выполнить тесты для каждой атаки
    4. Собрать результаты
    5. Сгенерировать отчет
  
  Время: 4-6 часов (0.5 дня)
  Файл: recon/test_all_attacks.py

ФАЗА 4: СОЗДАТЬ СПЕЦИФИКАЦИИ АТАК
  
  Задачи:
    1. Документировать все атаки в YAML
    2. Определить ожидаемые пакеты
    3. Определить правила валидации
    4. Добавить тестовые вариации
  
  Время: 4-6 часов (0.5 дня)
  Директория: recon/specs/attacks/

ФАЗА 5: ИНТЕГРАЦИОННОЕ ТЕСТИРОВАНИЕ
  
  Задачи:
    1. Протестировать все атаки end-to-end
    2. Валидировать против реальных PCAP
    3. Исправить найденные проблемы
    4. Сгенерировать финальный отчет
  
  Время: 4-6 часов (0.5 дня)

┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚡ БЫСТРЫЙ СТАРТ (СДЕЛАТЬ СЕГОДНЯ)                                           │
└──────────────────────────────────────────────────────────────────────────────┘

1️⃣  ИСПРАВИТЬ ПАРСЕР (2 часа)
    
    Создать: recon/core/strategy_parser_v2.py
    
    Функционал:
      • Распознавать function-style: fake(ttl=1, fooling=['badsum'])
      • Распознавать zapret-style: --dpi-desync=fake --dpi-desync-ttl=1
      • Автоматически определять формат
      • Конвертировать между форматами
      • Валидировать параметры
    
    Пример кода:
    ```python
    class StrategyParserV2:
        def parse(self, strategy: str) -> Dict:
            if '(' in strategy:
                return self._parse_function_style(strategy)
            elif '--dpi-desync' in strategy:
                return self._parse_zapret_style(strategy)
            else:
                raise ValueError(f"Unknown syntax: {strategy}")
        
        def _parse_function_style(self, strategy: str) -> Dict:
            # Parse: fake(ttl=1, fooling=['badsum'])
            match = re.match(r'(\w+)\((.*)\)', strategy)
            attack_name = match.group(1)
            params_str = match.group(2)
            params = self._parse_parameters(params_str)
            return {'type': attack_name, 'params': params}
    ```

2️⃣  ПРОТЕСТИРОВАТЬ ПАРСЕР (30 минут)
    
    Тесты:
      • fake(ttl=1) → {'type': 'fake', 'params': {'ttl': 1}}
      • split(split_pos=1) → {'type': 'split', 'params': {'split_pos': 1}}
      • fakeddisorder(split_pos=76, ttl=3) → {...}
    
    Команда:
    ```bash
    python -c "from recon.core.strategy_parser_v2 import StrategyParserV2; \
               parser = StrategyParserV2(); \
               print(parser.parse('fake(ttl=1, fooling=[\"badsum\"])'))"
    ```

3️⃣  СОЗДАТЬ ПРОСТОЙ ВАЛИДАТОР (2 часа)
    
    Создать: recon/core/packet_validator.py
    
    Функционал:
      • Парсить PCAP файлы
      • Извлекать пакеты
      • Валидировать sequence numbers
      • Валидировать checksums
      • Валидировать TTL
      • Генерировать отчет
    
    Пример кода:
    ```python
    class PacketValidator:
        def validate_attack(self, attack_name: str, params: Dict, pcap_file: str):
            packets = self._parse_pcap(pcap_file)
            
            # Validate seq numbers
            if attack_name == 'fakeddisorder':
                fake_seq = packets[0].seq
                real_seq = packets[1].seq
                assert fake_seq == real_seq, f"Seq mismatch: {fake_seq} != {real_seq}"
            
            # Validate checksums
            if 'badsum' in params.get('fooling', []):
                assert not packets[0].checksum_valid, "Fake packet should have bad checksum"
            
            # Validate TTL
            expected_ttl = params.get('ttl', 1)
            assert packets[0].ttl == expected_ttl, f"TTL mismatch: {packets[0].ttl} != {expected_ttl}"
    ```

4️⃣  ЗАПУСТИТЬ ВАЛИДАЦИЮ НА СУЩЕСТВУЮЩИХ PCAP (1 час)
    
    Файлы для проверки:
      • test_fix_fakeddisorder.pcap
      • test_fix_fake_ttl1.pcap
      • test_fix_split_sni.pcap
      • test_extreme_split1.pcap
      • test_extreme_ttl64.pcap
      • test_extreme_overlap.pcap
    
    Команда:
    ```bash
    python -c "from recon.core.packet_validator import PacketValidator; \
               validator = PacketValidator(); \
               result = validator.validate_attack('fakeddisorder', \
                   {'split_pos': 76, 'ttl': 3, 'fooling': ['badsum']}, \
                   'test_fix_fakeddisorder.pcap'); \
               print(result)"
    ```

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📋 СПЕЦИФИКАЦИИ АТАК                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

FAKE ATTACK:
  
  Синтаксис:
    fake(ttl=1, fooling=['badsum'], fake_sni='example.com')
  
  Ожидаемые пакеты:
    1. Fake packet:
       - TTL: <ttl>
       - Checksum: bad if 'badsum' in fooling
       - Seq: <original_seq>
    
    2. Real packet:
       - TTL: 64 or 128
       - Checksum: good
       - Seq: <original_seq>
  
  Правила валидации:
    ✓ fake_packet.ttl == params.ttl
    ✓ fake_packet.seq == real_packet.seq
    ✓ if 'badsum' in fooling: fake_packet.checksum_valid == False
    ✓ real_packet.checksum_valid == True

SPLIT ATTACK:
  
  Синтаксис:
    split(split_pos=1)
  
  Ожидаемые пакеты:
    1. First part:
       - Length: <split_pos>
       - Seq: <original_seq>
    
    2. Second part:
       - Length: <total_length - split_pos>
       - Seq: <original_seq + split_pos>
  
  Правила валидации:
    ✓ first_part.length == params.split_pos
    ✓ second_part.seq == first_part.seq + first_part.length
    ✓ first_part.payload + second_part.payload == original_payload

FAKEDDISORDER ATTACK:
  
  Синтаксис:
    fakeddisorder(split_pos=76, overlap_size=336, ttl=3, fooling=['badsum'])
  
  Ожидаемые пакеты:
    1. Fake packet:
       - TTL: <ttl>
       - Seq: <original_seq>  ← КРИТИЧНО!
       - Checksum: bad if 'badsum' in fooling
    
    2. Real part 2 (отправляется первым):
       - Seq: <original_seq + split_pos>
       - Length: <total_length - split_pos>
    
    3. Real part 1 (отправляется вторым):
       - Seq: <original_seq>
       - Length: <split_pos>
  
  Правила валидации:
    ✓ fake_packet.seq == real_part1.seq  ← КРИТИЧНО!
    ✓ real_part2.seq == real_part1.seq + real_part1.length
    ✓ if overlap_size > 0: real_part2.seq < real_part1.seq + real_part1.length

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

ПОСЛЕ ФАЗЫ 1 (Исправление парсера):
  ✅ Все стратегии парсятся корректно
  ✅ Тесты могут запускаться
  ✅ Пакеты генерируются

ПОСЛЕ ФАЗЫ 2 (Валидатор пакетов):
  ✅ Sequence numbers валидируются
  ✅ Checksums валидируются
  ✅ TTL валидируется
  ✅ Ошибки детектируются

ПОСЛЕ ФАЗЫ 3 (Оркестратор):
  ✅ Все атаки тестируются автоматически
  ✅ Результаты собираются
  ✅ Отчет генерируется

ПОСЛЕ ФАЗЫ 4 (Спецификации):
  ✅ Все атаки документированы
  ✅ Правила валидации определены
  ✅ Тестовые вариации добавлены

ПОСЛЕ ФАЗЫ 5 (Интеграция):
  ✅ Все атаки работают корректно
  ✅ Пакеты соответствуют спецификациям
  ✅ Финальный отчет готов

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📁 СОЗДАННЫЕ ФАЙЛЫ СПЕЦИФИКАЦИИ                                              │
└──────────────────────────────────────────────────────────────────────────────┘

1. .kiro/specs/attack-validation-suite/requirements.md
   • User stories
   • Acceptance criteria
   • Technical requirements
   • Success metrics

2. .kiro/specs/attack-validation-suite/design.md
   • Architecture overview
   • Root cause analysis
   • Solution design
   • Implementation plan
   • Attack specifications

3. .kiro/specs/attack-validation-suite/tasks.md
   • Detailed tasks for all 5 phases
   • Quick start tasks
   • Success criteria
   • Timeline estimates

4. ПЛАН_ТЕСТИРОВАНИЯ_АТАК.txt (этот файл)
   • Краткая сводка на русском
   • Быстрый старт
   • Спецификации атак

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚀 СЛЕДУЮЩИЕ ШАГИ                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

СЕГОДНЯ (5-6 часов):
  1. Создать StrategyParserV2 (2 часа)
  2. Протестировать парсер (30 минут)
  3. Создать PacketValidator (2 часа)
  4. Запустить валидацию на существующих PCAP (1 час)

НА ЭТОЙ НЕДЕЛЕ (10-15 часов):
  5. Создать спецификации для топ-10 атак (4 часа)
  6. Реализовать оркестратор тестов (3 часа)
  7. Запустить полный набор тестов (2 часа)
  8. Сгенерировать комплексный отчет (1 час)

В ЭТОМ МЕСЯЦЕ (20-30 часов):
  9. Документировать все атаки
  10. Исправить все найденные проблемы
  11. Создать регрессионные тесты
  12. Интегрировать с CI/CD

╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   ВЫВОД: КРИТИЧЕСКАЯ ПРОБЛЕМА ПАРСЕРА БЛОКИРУЕТ ВСЕ ТЕСТИРОВАНИЕ            ║
║                                                                              ║
║   ДЕЙСТВИЕ: ИСПРАВИТЬ ПАРСЕР → СОЗДАТЬ ВАЛИДАТОР → ПРОТЕСТИРОВАТЬ ВСЕ       ║
║                                                                              ║
║   НАЧАТЬ С: Создание StrategyParserV2                                       ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

Дата создания: 2025-10-04
Версия: 1.0
Статус: 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА
Приоритет: 🔥🔥🔥 БЛОКИРУЮЩАЯ
Следующий шаг: СОЗДАТЬ STRATEGY_PARSER_V2
Время на исправление: 5-6 часов (сегодня)
