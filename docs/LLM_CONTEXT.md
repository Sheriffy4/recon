# LLM Context для проекта Recon

**Сгенерировано:** 2025-12-23 12:30:13

Этот документ содержит контекст для работы LLM с проектом Recon - инструментом для обхода DPI блокировок.

## Обзор проекта

Recon - это комплексный инструмент для обхода DPI (Deep Packet Inspection) блокировок. Проект включает:

- **Движок обхода блокировок** с различными стратегиями атак
- **Анализ сетевого трафика** и захват пакетов
- **Автоматическое обнаружение** оптимальных стратегий
- **CLI и сервисные режимы** работы
- **Мониторинг и диагностику** эффективности

## Правила работы с проектом

### 🔍 ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ПЕРЕД СОЗДАНИЕМ КОДА

1. **ВСЕГДА проверяй MODULE_REGISTRY.md** перед созданием нового функционала
   - Ссылка: [MODULE_REGISTRY.md](MODULE_REGISTRY.md)
   - Убедись что функционал не дублируется

2. **ВСЕГДА проверяй PROJECT_STRUCTURE.md** чтобы понять где размещать код
   - Ссылка: [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)
   - Следуй существующей структуре директорий

3. **НЕ создавай дублирующий функционал** - особенно для валидации, сетевых операций, работы с файлами

### 📁 Размещение нового кода

- **Атаки и обход**: `core/bypass/attacks/`
- **Сетевые операции**: `core/network/`, `core/pcap/`
- **Конфигурация**: `core/config/`
- **Валидация**: `core/validation/`
- **CLI утилиты**: корень проекта или `core/cli/`
- **Тесты**: `tests/`

### 🧪 Тестирование

- Используй существующие тестовые утилиты из категории Testing
- Создавай property-based тесты для критичного функционала
- Размещай тесты в директории `tests/`

## Быстрая справка по функционалу

### Основные категории (10 категорий)

#### Attack (0 модулей)

**Описание:** Модули для реализации атак обхода DPI блокировок

**Примеры функционала:**
- TCP fragmentation
- TLS evasion
- HTTP header manipulation
- Packet splitting
- Timing attacks

**Ключевые модули:**
- `core/bypass/attacks/base.py`
- `core/bypass/attacks/attack_registry.py`
- `core/bypass/attacks/combo/`

**Как использовать:**
- Создание новой атаки: наследуйся от BaseAttack
- Регистрация атаки: используй @register_attack декоратор
- Комбинированные атаки: используй модули из combo/

#### Cli (0 модулей)

**Описание:** Интерфейс командной строки, CLI утилиты

**Примеры функционала:**
- Command parsing
- CLI workflows
- Interactive modes
- Batch processing
- CLI tools

**Ключевые модули:**
- `cli.py`
- `core/cli/`
- `cli_*.py`
- `simple_cli.py`

**Как использовать:**
- Основной CLI: cli.py - главный интерфейс
- Простой CLI: simple_cli.py для базовых операций
- CLI утилиты: cli_*.py скрипты для специфических задач

#### Config (0 модулей)

**Описание:** Конфигурация приложения, настройки, параметры

**Примеры функционала:**
- Config loading
- Settings management
- Parameter validation
- Config migration
- Environment setup

**Ключевые модули:**
- `core/config/`
- `config.py`
- `settings/`
- `*.json configs`

**Как использовать:**
- Загрузка конфигов: core/config/enhanced_config.py
- Миграция конфигов: core/config/config_migration_tool.py
- Валидация настроек: core/config/strategy_validator.py

#### Files (0 модулей)

**Описание:** Работа с файлами, конфигурациями, логами

**Примеры функционала:**
- Config loading
- Log parsing
- File validation
- JSON processing
- File I/O operations

**Ключевые модули:**
- `core/config/`
- `core/logging/`
- `utils/`
- `file_*.py`

**Как использовать:**
- Загрузка конфигов: core/config/config_loader.py
- Парсинг логов: improved_log_parser.py
- Валидация файлов: validate_*.py скрипты

#### Logging (0 модулей)

**Описание:** Логирование, мониторинг, диагностика

**Примеры функционала:**
- Event logging
- Performance monitoring
- Debug output
- Structured logging
- Log analysis

**Ключевые модули:**
- `core/logging/`
- `core/monitoring/`
- `logs/`
- `monitor_*.py`

**Как использовать:**
- Мониторинг: monitor.py, intelligent_bypass_monitor.py
- Логирование: core/discovery_logging.py
- Диагностика: doctor.py, diagnose_*.py скрипты

#### Network (0 модулей)

**Описание:** Сетевые операции, анализ трафика, работа с пакетами

**Примеры функционала:**
- Packet capture
- Traffic analysis
- Connection testing
- PCAP processing
- Network monitoring

**Ключевые модули:**
- `core/pcap/`
- `core/network/`
- `core/connection/`
- `pcap_*.py`

**Как использовать:**
- Захват трафика: используй модули из core/pcap/
- Анализ пакетов: pcap_analyzer.py, pcap_strategy_analyzer.py
- Тестирование соединений: connection_tester, network_behavior_analyzer

#### Other (0 модулей)

**Описание:** Прочие модули и утилиты общего назначения

**Примеры функционала:**
- Utility functions
- Helper modules
- Common tools
- Miscellaneous scripts

**Ключевые модули:**
- `utils/`
- `helpers/`
- `misc scripts`

**Как использовать:**
- Утилиты: utils/ директория
- Вспомогательные скрипты: различные helper модули
- Общие инструменты: shared utilities

#### Strategy (0 модулей)

**Описание:** Стратегии обхода, генерация и применение стратегий

**Примеры функционала:**
- Strategy generation
- Strategy optimization
- Strategy selection
- Adaptive strategies
- Strategy combination

**Ключевые модули:**
- `core/strategy/`
- `core/bypass/strategies/`
- `strategy_*.py`
- `adaptive_*.py`

**Как использовать:**
- Генерация стратегий: core/strategy/strategy_generator.py
- Адаптивные стратегии: adaptive_strategy_finder.py
- Оптимизация: core/strategy/strategy_parameter_optimizer.py

#### Testing (0 модулей)

**Описание:** Тестирование функционала, валидация, проверки

**Примеры функционала:**
- Unit tests
- Integration tests
- Property-based tests
- Performance tests
- Validation tests

**Ключевые модули:**
- `tests/`
- `core/validation/`
- `test_*.py`
- `validate_*.py`

**Как использовать:**
- Unit тесты: tests/test_*.py файлы
- Property-based тесты: используй Hypothesis
- Интеграционные тесты: test_*_integration.py

#### Validation (0 модулей)

**Описание:** Валидация данных, проверка корректности, верификация

**Примеры функционала:**
- Strategy validation
- Config validation
- Data integrity
- Input validation
- Schema validation

**Ключевые модули:**
- `core/validation/`
- `validators/`
- `core/verify/`
- `validate_*.py`

**Как использовать:**
- Валидация стратегий: validate_strategy_names.py
- Проверка конфигов: core/config/strategy_validator.py
- Верификация данных: core/validation/results_validation_system.py

## Основные точки входа

Главные исполняемые файлы проекта:

- **_service.py** - Сервисный режим работы
- **abs_twimg_analysis_and_fix.py** - Исполняемый скрипт
- **adaptive_bypass_service.py** - Сервисный режим работы
- **adaptive_strategy_finder.py** - Исполняемый скрипт
- **aggressive_strategy_tester.py** - Тестовый скрипт
- **alternative_capture_solution.py** - Захват и анализ трафика
- **analyze_last_flow.py** - Исполняемый скрипт
- **analyze_pcap_simple.py** - Исполняемый скрипт
- **attack_handlers_stub.py** - Исполняемый скрипт
- **attack_parity_cli.py** - Интерфейс командной строки

*И еще 308 entry points - см. PROJECT_STRUCTURE.md*


## Быстрая справка по общим задачам

### 🔧 Частые задачи разработки

#### Создание новой атаки
1. Проверь MODULE_REGISTRY.md - возможно атака уже существует
2. Наследуйся от `BaseAttack` из `core.bypass.attacks.base`
3. Используй декоратор `@register_attack`
4. Реализуй методы `name()`, `execute()`, `category()`

#### Анализ сетевого трафика
1. Для захвата: используй модули из `core/pcap/`
2. Для анализа: `pcap_analyzer.py`, `pcap_strategy_analyzer.py`
3. Для мониторинга: `pcap_monitor.py`, `network_behavior_analyzer.py`

#### Работа с конфигурацией
1. Загрузка: `core/config/enhanced_config.py`
2. Валидация: `core/config/strategy_validator.py`
3. Миграция: `core/config/config_migration_tool.py`

#### Тестирование
1. Unit тесты: создавай в `tests/test_*.py`
2. Property-based: используй Hypothesis
3. Интеграционные: `test_*_integration.py`

#### Валидация данных
1. Стратегии: `validate_strategy_names.py`
2. Конфиги: `core/config/strategy_validator.py`
3. Общая валидация: `core/validation/`

### 🎯 Специфичные сценарии

#### Отладка проблем с обходом
1. Используй `doctor.py` для диагностики
2. Анализируй логи с `improved_log_parser.py`
3. Сравнивай CLI vs Service с `cli_service_mode_comparator.py`

#### Оптимизация стратегий
1. Генерация: `core/strategy/strategy_generator.py`
2. Оптимизация: `core/strategy/strategy_parameter_optimizer.py`
3. Адаптивные: `adaptive_strategy_finder.py`

#### Мониторинг производительности
1. Основной мониторинг: `monitor.py`
2. Интеллектуальный: `intelligent_bypass_monitor.py`
3. Производительность: `performance_monitor_script.py`

### 📊 Анализ и отчеты
1. Глубокий анализ: `deep_attack_analysis.py`
2. Сравнение стратегий: `deep_strategy_comparison_analyzer.py`
3. Генерация отчетов: `generate_service_analysis_report.py`



## Примеры использования

### Создание новой атаки

```python
# СНАЧАЛА проверь MODULE_REGISTRY.md - возможно атака уже существует!
from core.bypass.attacks.base import BaseAttack
from core.bypass.attacks.attack_registry import register_attack

@register_attack
class MyNewAttack(BaseAttack):
    def name(self):
        return "my_new_attack"
    
    def execute(self, context):
        # Реализация атаки
        pass
```

### Работа с конфигурацией

```python
# Используй существующие утилиты из категории Config
from core.config import load_config, validate_config

config = load_config("my_config.json")
if validate_config(config):
    # Работа с конфигом
    pass
```

### Валидация данных

```python
# Используй существующие валидаторы из категории Validation
from core.validation import validate_strategy, validate_domain

if validate_strategy(strategy_data):
    # Стратегия корректна
    pass
```

## Архитектурные принципы

1. **Модульность** - каждый компонент решает одну задачу
2. **Переиспользование** - не дублируй существующий функционал
3. **Тестируемость** - покрывай код тестами
4. **Документированность** - документируй публичные API
5. **Безопасность** - валидируй входные данные

## Полезные ссылки

- [MODULE_REGISTRY.md](MODULE_REGISTRY.md) - Полный реестр модулей
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура проекта
- `tests/` - Примеры тестов
- `examples/` - Примеры использования
- `docs/` - Дополнительная документация

---

**Помни:** Этот проект большой и сложный. Всегда проверяй существующий функционал перед созданием нового!
