# КРИТИЧЕСКИЙ АНАЛИЗ: Почему recon открывает 0 доменов, а zapret 27

## 🚨 ОСНОВНЫЕ ПРОБЛЕМЫ НАЙДЕНЫ

### 1. **КРИТИЧЕСКАЯ РАЗНИЦА В ПАРАМЕТРАХ СТРАТЕГИИ**

**Recon стратегия:**
```
fakeddisorder(overlap_size=1, split_pos=76, ttl=64, autottl=2, fooling=['badseq', 'md5sig'])
```

**Zapret рабочая стратегия (27/31 успехов):**
```
--dpi-desync=fakeddisorder --dpi-desync-split-seqovl=336 --dpi-desync-autottl=2 --dpi-desync-fooling=md5sig,badsum,badseq --dpi-desync-split-pos=76 --dpi-desync-ttl=1
```

### 2. **КЛЮЧЕВЫЕ РАЗЛИЧИЯ В ПАРАМЕТРАХ**

| Параметр | Recon | Zapret (рабочий) | Проблема |
|----------|-------|------------------|----------|
| `overlap_size` / `split-seqovl` | **1** | **336** | ❌ КРИТИЧНО! |
| `ttl` | **64** | **1** | ❌ КРИТИЧНО! |
| `fooling` | `['badseq', 'md5sig']` | `['md5sig', 'badsum', 'badseq']` | ❌ Отсутствует `badsum` |

### 3. **АНАЛИЗ ТРАФИКА ПОКАЗЫВАЕТ ПРОБЛЕМЫ**

**Recon PCAP:**
- Всего пакетов: **285** (очень мало!)
- Fake пакетов: **6** (почти нет!)
- TTL значения: `{54: 52, 128: 174, 57: 22, 121: 22, 53: 2, 107: 1, 64: 12}`

**Zapret PCAP:**
- Всего пакетов: **18,528** (в 65 раз больше!)
- Fake пакетов: **897** (в 150 раз больше!)
- TTL значения: много разных значений включая `{1: 691, 2: 116, 3: 251, 4: 41, 5: 153, ...}`

### 4. **РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ**

- **Recon**: 0/26 доменов работают (0% успех)
- **Zapret**: 27/31 доменов работают (87% успех)
- **Разница**: 27 доменов не работают в recon!

## 🔧 КОРНЕВЫЕ ПРИЧИНЫ ПРОБЛЕМ

### 1. **Неправильный `overlap_size` (split-seqovl)**
- **Recon использует**: `overlap_size=1` 
- **Zapret использует**: `split-seqovl=336`
- **Проблема**: Это критический параметр для fakeddisorder атаки!

### 2. **Неправильный TTL**
- **Recon использует**: `ttl=64` (наше "улучшение" из Task 3)
- **Zapret использует**: `ttl=1` (оригинальное значение)
- **Проблема**: TTL=64 не работает для этой DPI системы!

### 3. **Отсутствует `badsum` fooling**
- **Recon**: только `['badseq', 'md5sig']`
- **Zapret**: `['md5sig', 'badsum', 'badseq']`
- **Проблема**: `badsum` может быть критичным для обхода

### 4. **Недостаточная генерация fake пакетов**
- Recon генерирует в 150 раз меньше fake пакетов
- Это указывает на проблемы в реализации атаки

## 🎯 КОНКРЕТНЫЕ ИСПРАВЛЕНИЯ

### Исправление 1: Параметр `overlap_size`
```python
# В strategy_interpreter.py, метод _build_fakeddisorder_params
# БЫЛО:
params["overlap_size"] = 336  # Default

# ДОЛЖНО БЫТЬ (когда split-seqovl=1):
if parsed.split_seqovl is not None:
    params["overlap_size"] = parsed.split_seqovl  # Используем точное значение!
```

### Исправление 2: TTL для fakeddisorder
```python
# В strategy_interpreter_fixed.py
# БЫЛО (Task 3):
self.ttl = 64  # TASK 3: Changed from 1 to 64

# ДОЛЖНО БЫТЬ:
self.ttl = 1  # Вернуть оригинальное значение для fakeddisorder!
```

### Исправление 3: Добавить `badsum` fooling
```python
# Проверить, что все fooling методы из zapret поддерживаются
fooling_methods = ['md5sig', 'badsum', 'badseq']
```

## 🚨 КРИТИЧЕСКИЙ ВЫВОД

**Проблема НЕ в Task 3 (TTL validation), а в неправильной интерпретации параметров стратегии!**

1. **`overlap_size=1` вместо `336`** - это основная причина
2. **TTL=64 вместо TTL=1** - вторая критическая проблема  
3. **Отсутствие `badsum`** - дополнительная проблема

## 📋 ПЛАН ИСПРАВЛЕНИЯ

1. **Немедленно**: Исправить `overlap_size` в стратегии
2. **Немедленно**: Вернуть TTL=1 для fakeddisorder (откатить Task 3 частично)
3. **Проверить**: Поддержку всех fooling методов
4. **Протестировать**: С исправленными параметрами

## 🔍 КОМАНДА ДЛЯ ТЕСТИРОВАНИЯ

После исправлений протестировать с точными параметрами zapret:
```bash
python cli.py -d sites.txt --strategy "--dpi-desync=fakeddisorder --dpi-desync-split-seqovl=336 --dpi-desync-autottl=2 --dpi-desync-fooling=md5sig,badsum,badseq --dpi-desync-repeats=1 --dpi-desync-split-pos=76 --dpi-desync-ttl=1" --pcap out_fixed.pcap
```

Это должно дать результат близкий к zapret: 27/31 доменов работают.