# Требования: Рефакторинг Диспетчеризации Атак DPI

## 📋 Обзор Проблемы

### Текущая Критическая Проблема
В `base_engine.py::apply_bypass()` все различные типы атак обрабатываются одинаково:

```python
if task_type in ("fakeddisorder", "multidisorder", "disorder", "disorder2", "seqovl"):
    # ❌ ВСЕ 5 РАЗНЫХ АТАК ВЫПОЛНЯЮТСЯ КАК fakeddisorder!
    recipe = self.techniques.apply_fakeddisorder(...)
```

**Результат**: 
- `seqovl` работает как `fakeddisorder` ❌
- `multidisorder` работает как `fakeddisorder` ❌  
- Параметры `overlap_size`, `positions` игнорируются ❌
- Уникальная логика каждой атаки не используется ❌

## 🎯 Цели Рефакторинга

### Основные Цели
1. **Правильная диспетчеризация**: Каждый тип атаки должен вызывать свой метод
2. **Использование всех модулей**: Задействовать папку `core/bypass/attacks/`
3. **Унификация архитектуры**: Создать единую систему управления атаками
4. **Документация**: Полная карта всех модулей проекта

### Пользовательские Истории

#### US-1: Правильная Диспетчеризация Атак
**Как** разработчик DPI обхода  
**Я хочу** чтобы каждый тип атаки выполнялся своим специализированным методом  
**Чтобы** получить максимальную эффективность каждой стратегии

**Критерии приемки:**
- `fakeddisorder` → `apply_fakeddisorder()`
- `seqovl` → `apply_seqovl()` с параметром `overlap_size`
- `multidisorder` → `apply_multidisorder()` с параметром `positions`
- `disorder` → `apply_disorder()` (без фейкового пакета)
- `multisplit` → `apply_multisplit()` с параметром `positions`

#### US-2: Использование Модулей Attacks
**Как** разработчик  
**Я хочу** использовать готовые модули из `core/bypass/attacks/`  
**Чтобы** не дублировать функциональность

**Критерии приемки:**
- Инвентаризация всех модулей в `core/bypass/attacks/`
- Интеграция существующих атак в диспетчер
- Удаление дублирующего кода

#### US-3: Поддержка Специальных Параметров
**Как** пользователь  
**Я хочу** использовать специальные значения `split_pos` (`cipher`, `sni`, `midsld`)  
**Чтобы** атаки автоматически находили оптимальные позиции разделения

**Критерии приемки:**
- `split_pos=cipher` → автоматическое определение позиции cipher suite
- `split_pos=sni` → автоматическое определение позиции SNI
- `split_pos=midsld` → автоматическое определение средней позиции

#### US-4: Архитектурная Документация
**Как** разработчик  
**Я хочу** иметь полную карту всех модулей проекта  
**Чтобы** избежать создания дублирующих модулей

**Критерии приемки:**
- Документ с описанием всех модулей
- Схема взаимодействия компонентов
- Руководство по добавлению новых атак

## 🔧 Технические Требования

### TR-1: Рефакторинг apply_bypass()
- Заменить единый блок `if task_type in (...)` на отдельные блоки для каждой атаки
- Добавить helper методы для извлечения параметров
- Обеспечить правильную передачу параметров каждому типу атаки

### TR-2: Расширение primitives.py
- Добавить недостающие методы (`apply_disorder`, `apply_seqovl` если отсутствуют)
- Стандартизировать интерфейсы всех методов атак
- Добавить логирование для каждого типа атаки

### TR-3: Интеграция с Модулями Attacks
- Создать единый реестр атак
- Интегрировать существующие модули из `core/bypass/attacks/`
- Создать фабрику атак для динамического создания

### TR-4: Улучшение CLI Генерации
- Исправить `genes_to_zapret_strategy()` для правильной генерации команд
- Обновить `_extract_strategy_type()` с приоритетом паттернов
- Добавить валидацию параметров для каждого типа

## 📊 Метрики Успеха

### Функциональные Метрики
- [ ] Все типы атак диспетчеризуются правильно (100%)
- [ ] Специальные параметры обрабатываются корректно
- [ ] Нет дублирования кода между модулями
- [ ] Все существующие тесты проходят

### Качественные Метрики  
- [ ] Код легко расширяется новыми типами атак
- [ ] Архитектура понятна новым разработчикам
- [ ] Документация покрывает все компоненты
- [ ] Производительность не ухудшилась

## 🚫 Ограничения

### Обратная Совместимость
- Все существующие стратегии должны продолжать работать
- API для внешних модулей не должен измениться
- Конфигурационные файлы остаются совместимыми

### Производительность
- Время диспетчеризации не должно увеличиться более чем на 10%
- Потребление памяти не должно значительно возрасти
- Телеметрия должна продолжать работать

## 🔄 Этапы Реализации

### Этап 1: Анализ и Документация (1-2 дня)
- Инвентаризация всех модулей в проекте
- Создание архитектурной документации
- Анализ существующих атак в `core/bypass/attacks/`

### Этап 2: Рефакторинг Диспетчера (2-3 дня)
- Рефакторинг `apply_bypass()` с правильной диспетчеризацией
- Добавление helper методов
- Расширение `primitives.py`

### Этап 3: Интеграция Модулей (2-3 дня)
- Создание единого реестра атак
- Интеграция существующих модулей
- Создание фабрики атак

### Этап 4: Тестирование и Валидация (1-2 дня)
- Создание тестов для каждого типа атаки
- Проверка обратной совместимости
- Валидация производительности

## 🧪 План Тестирования

### Модульные Тесты
- Тест диспетчеризации для каждого типа атаки
- Тест обработки специальных параметров
- Тест интеграции с модулями attacks

### Интеграционные Тесты
- Тест полного цикла выполнения атак
- Тест совместимости с CLI
- Тест работы с реальными стратегиями

### Регрессионные Тесты
- Все существующие тесты должны проходить
- Тест производительности
- Тест обратной совместимости

## 📝 Документация

### Обязательная Документация
- [ ] Архитектурная схема всех модулей
- [ ] Руководство по добавлению новых атак
- [ ] API документация для каждого типа атаки
- [ ] Примеры использования всех параметров

### Дополнительная Документация
- [ ] Диаграммы последовательности для каждой атаки
- [ ] Сравнительная таблица всех типов атак
- [ ] Руководство по миграции для разработчиков